void main() {
    IO.print("Enter you want box :");
    int box = Integer.parseInt(IO.readln()) ;
    IO.print("Enter you you want pencil :");
    int pencil = Integer.parseInt(IO.readln()) ;
    int anwser = box * pencil ;
    IO.println(" box is " + box + " Pencil is " + pencil + " anwser = " +  anwser );
} 
